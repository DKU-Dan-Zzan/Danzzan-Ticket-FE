#!/bin/sh

echo "Running color token policy check (staged files)..."
npm run lint:colors:staged
status=$?

if [ $status -ne 0 ]; then
  echo ""
  echo "Commit blocked by color policy. Use tokens or add explicit allowlist with reason."
  exit $status
fi

exit 0
